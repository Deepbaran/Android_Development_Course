fun main() {
    // Type Conversion
    val a: Int = 10
    val b: Long = a.toLong() // Kotlin does not support implicit conversion of data types. So, we need to do explicit conversion
    println(b)
}